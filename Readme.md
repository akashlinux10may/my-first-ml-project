# My First ML Project

A simple ML pipeline project to learn Git and team collaboration.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run training: `python model_training.py`
3. Run inference: `python main.py`